import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ──────────────────────────────────────────────────────────────────
  static const Color primary = Color(0xFF003566);
  static const Color primaryLight = Color(0xFF1652A8);
  static const Color accent = Color(0xFFE63946);

  // ── Backgrounds ────────────────────────────────────────────────────────────
  static const Color background = Color(0xFFF0F2F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF7F8FA);
  static const Color matchCardBackground = Color(0xFF06113D);

  // ── Match Status ───────────────────────────────────────────────────────────
  static const Color live = Color(0xFFE63946);
  static const Color upcoming = Color(0xFF0A84FF);
  static const Color finished = Color(0xFF94A3B8);

  // ── Sport Identity ─────────────────────────────────────────────────────────
  static const Color football = Color(0xFF003566);
  static const Color footballLight = Color(0xFFE8EEFF);
  static const Color cricket = Color(0xFF1B5E35);
  static const Color cricketLight = Color(0xFFE8F5EE);
  static const Color news = Color(0xFF6D28D9);
  static const Color newsLight = Color(0xFFF3E8FF);

  // ── Text ───────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ── Navigation & Drawer ────────────────────────────────────────────────────
  static const Color navBackground = Color(0xFFFFFFFF);
  static const Color drawerBackground = Color(0xFF06113D);
  static const Color drawerGradientStart = Color(0xFF06113D);
  static const Color drawerGradientEnd = Color(0xFF162455);
  static const Color activeTab = Color(0xFF003566);
  static const Color inactiveTab = Color(0xFF94A3B8);
  static const Color bottomNavBackground = Color(0xFFFFFFFF);

  // ── Social ─────────────────────────────────────────────────────────────────
  static const Color telegram = Color(0xFF0088CC);
  static const Color whatsapp = Color(0xFF25D366);

  // ── UI Chrome ──────────────────────────────────────────────────────────────
  static const Color divider = Color(0xFFEEF0F4);
  static const Color border = Color(0xFFE2E8F0);
}
