import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Premium Professional Light/Navy Theme
  static const Color primary = Color(0xFF003566); // Deep Navy Blue
  static const Color accent = Color(0xFFE63946); // Energetic Red
  
  static const Color background = Color(0xFFF8F9FA); // Very Light Gray
  static const Color surface = Color(0xFFFFFFFF); // Pure White

  // Match Card
  static const Color matchCardBackground = Color(0xFF050E40);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1D1D1F); // Near Black
  static const Color textSecondary = Color(0xFF6E6E73); // System Gray
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Navigation & Branding
  static const Color navBackground = Color(0xFFFFFFFF);
  static const Color drawerBackground = Color(0xFF0D1B2A); // Keep Navy for Contrast
  static const Color drawerGradientStart = Color(0xFF0D1B2A);
  static const Color drawerGradientEnd = Color(0xFF1B2A47);

  // Social
  static const Color telegram = Color(0xFF0088CC);
  static const Color whatsapp = Color(0xFF25D366);

  // Bottom Navigation
  static const Color bottomNavBackground = Color(0xFFFFFFFF);
  static const Color activeTab = Color(0xFF003566);
  static const Color inactiveTab = Color(0xFF8E8E93);
}
